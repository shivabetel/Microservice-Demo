server:
  port: 8083
spring:
  application:
    name: customerordermanagement
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${eureka.server}